# CONSTRUCTION PLAN — bank-deposit-registry

Companion to `BLUEPRINT-bank-deposit-registry.md`. วิธีทำงาน: สร้าง → ทดสอบ → ผ่าน (create → test → pass)
Output target: Code.gs (GAS) ของระบบออมทรัพย์ + index.html หน้า admin — ต้อง paste Code.gs เข้า GAS editor + redeploy + git push ตามขั้นตอนปกติของโปรเจกต์นี้ (ดู CLAUDE_HANDOFF.md)

**⚠️ PIVOT 2026-07-07**: Stage 1-11 ด้านล่างสร้างตาม spec รายวันดั้งเดิม (ship แล้วที่ `4a5d996`) แต่หลังทดสอบจริง Pam ยืนยันว่าต้องการ**รายเดือน**ไม่ใช่รายวัน — แก้ไขแล้วในโค้ดจริง (commit `db8dfb8` + Date-object fix `9b652e3`) ดูรายละเอียดที่หัวข้อ "PIVOT: รายวัน → รายเดือน" ใน BLUEPRINT ก่อนอ้างอิง stage ไหนด้านล่างนี้

**⚠️ PIVOT 2 2026-07-08 (grill เสร็จแล้ว, รอ approval)**: Stage 12-20 ด้านล่างคือของใหม่ที่ต้องสร้างต่อจาก v1.1 — แยกชั้น + กรองเฉพาะคนมีเลขบัญชี ดู BLUEPRINT หัวข้อ "PIVOT 2" สำหรับ spec เต็ม **ยังไม่ได้เขียนโค้ดสักบรรทัด รอ Pam อนุมัติก่อน**

---

## วิธีอ่านเอกสารนี้ (สำหรับ Pam)

แต่ละ Stage มี 4 ส่วน:
- 🔨 **สร้าง** — ผมจะทำอะไรในขั้นนี้
- 🧪 **ทดสอบ** — ผมเช็คยังไงว่าถูก (ก่อนให้ Pam ดู)
- 👁️ **Pam ดู** — สิ่งที่ผมจะโชว์ให้ Pam ตรวจ เป็นตัวอย่างจริงจากข้อมูลของ Pam ไม่ใช่ข้อมูลสมมติ
- ✅ **ผ่านเมื่อ** — เงื่อนไขที่ต้องผ่านก่อนไปขั้นถัดไป ถ้าไม่ผ่าน ผมแก้แล้วทดสอบใหม่ก่อน ไม่ข้ามขั้น

หน้าที่ของ Pam ระหว่างสร้าง: ตอบแค่ "ผ่าน" หรือ "ไม่ผ่านเพราะ..." ไม่ต้องอ่านโค้ด

## กฎทองของการสร้างรอบนี้

1. **ข้อมูลต้นทาง (sheet นักเรียน/ธุรกรรม) ห้ามแก้ไข/ลบเด็ดขาด** — อ่านอย่างเดียว
2. **ทุก Stage ต้องผ่านแบบไม่มี error ก่อนไปต่อ** — ไม่ปล่อยผ่านแบบ "น่าจะโอเค"
3. **ตรวจกับข้อมูลจริงของ Pam เสมอ** — ใช้ไฟล์ "ทะเบียนเงินสดฝากประจำวันโรงเรียนบ้านท่าชะอม กรกฎาคม 2569.xlsx" ที่วิเคราะห์ไปแล้วเป็นตัวเทียบ (ground truth ที่ธนาคารรับมาแล้วจริง)
4. **สร้าง engine (คำนวณให้ถูก) ก่อน interface (หน้าตาสวย)** — ถ้าตัวเลขผิด หน้าตาสวยแค่ไหนก็ใช้ไม่ได้ ถ้าเลขถูกแล้วหน้าตาไม่ตรง แก้แค่ styling ไม่ต้องสงสัยตัวเลขอีก
5. **แบ่ง stage เล็กพอที่ถ้าเจอบั๊ก จะรู้ทันทีว่าเกิดจาก stage ไหน**

## แผนที่ Stage

```
PHASE 1 FOUNDATION  — Stage 1-2: routing + auth gate, ดึง/กรองข้อมูลดิบให้ถูกก่อน
PHASE 2 ENGINE       — Stage 3-5: join ข้อมูล, คำนวณยอด, เรียงลำดับ (ยังไม่มีหน้าตา)
PHASE 3 INTERFACE    — Stage 6-8: สร้าง Sheet1, Sheet2, ปุ่ม+ฟอร์มฝั่ง admin
PHASE 4 HARDENING    — Stage 9-11: edge case, เทียบกับไฟล์จริงที่เคยส่งธนาคาร, deploy
```

---

## Stage 1 — Routing + admin password gate

🔨 **สร้าง**: เพิ่ม action `generateDepositRegistry` ใน `handleRequest()` switch + ฟังก์ชัน `generateDepositRegistry(p)` เปล่าๆ ที่แค่เช็ค `p.password !== ADMIN_PASSWORD` แล้ว return `{ok:true, stub:true}` ถ้ารหัสถูก

🧪 **ทดสอบ**: เรียกด้วยรหัสผิด → ต้องได้ `{ok:false, error:'เฉพาะผู้ดูแล'}` เรียกด้วยรหัสถูก → ต้องได้ `{ok:true, stub:true}`

👁️ **Pam ดู**: ผมจะรายงานผลทดสอบทั้ง 2 เคสเป็นข้อความสั้นๆ ให้ดู (ยังไม่มีอะไรให้กดในเว็บตอนนี้)

✅ **ผ่านเมื่อ**: ทั้ง 2 เคสตรงตามคาด ไม่มี error อื่นแทรก

---

## Stage 2 — แปลงวันที่ + ดึงธุรกรรม "ฝาก" เฉพาะวันที่เลือก (prefix match)

🔨 **สร้าง**: ฟังก์ชันย่อยแปลงวันที่ input → Thai date string ("D MMM YYYY" ไม่มีเวลา) + logic อ่าน sheet ธุรกรรม กรอง `ประเภท==='ฝาก'` และ `String(วันที่).indexOf(dateStr)===0` (prefix match เพราะ `วันที่` จริงมีเวลาต่อท้ายเสมอ เช่น "7 ก.ค. 2569 14:30" — ตรวจพบระหว่างอ่าน Code.gs จริงก่อนสร้าง แก้ BLUEPRINT แล้ว) — คืนแค่ raw list ของ transaction ที่ตรงเงื่อนไข ยังไม่ join กับนักเรียน

🧪 **ทดสอบ**: รันด้วยวันที่ที่รู้อยู่แล้วว่ามีข้อมูลจริง (จากไฟล์ตัวอย่างที่วิเคราะห์ไปคือ กรกฎาคม 2568 แต่ระบบจริงมีข้อมูลของปีปัจจุบัน 2569 — ใช้วันที่ที่ Pam ยืนยันว่ามีธุรกรรมฝากจริงในระบบตอนนี้) เช็คว่าจำนวนแถวที่ได้ตรงกับที่เปิด Google Sheet ดูเองด้วยตา

👁️ **Pam ดู**: ผมจะโชว์จำนวนธุรกรรมที่ดึงได้ + 3 แถวแรกเป็นตัวอย่าง ("เจอ N รายการฝากวันที่ ... เช่น เด็กชาย/เด็กหญิง ... จำนวน ... บาท") ให้ Pam เทียบกับที่เปิดดูใน Google Sheet เองตรงๆ

✅ **ผ่านเมื่อ**: จำนวน + ตัวอย่างข้อมูลตรงกับที่ Pam เช็คในชีตจริงด้วยตาเอง 100% (นี่คือจุดเสี่ยงสูงสุดของงานนี้ — ถ้าวันที่ match ผิด ทุกอย่างต่อจากนี้ผิดหมด)

---

## Stage 3 — Join กับข้อมูลนักเรียน (เลขบัญชี ธกส.)

🔨 **สร้าง**: ต่อจาก Stage 2 — join แต่ละ transaction กับ sheet นักเรียนด้วย `นักเรียน_id` เพื่อดึง `เลขบัญชี_ธกส` ถ้าหาไม่เจอ (นักเรียนถูกลบ/ไม่มีเลขบัญชี) ให้เว้นว่างแทนที่จะ error สร้าง array `rows[]` = `{no, bankAccount, name, amount}` (ยังไม่มี ลำดับที่ จริง แค่โครงสร้าง)

🧪 **ทดสอบ**: เช็คว่าทุกแถวมี `name` (ไม่ว่าง เพราะ transaction เก็บชื่อ snapshot ไว้เองอยู่แล้ว) เช็คว่า `bankAccount` ว่างเฉพาะกรณีที่คาดไว้เท่านั้น (นักเรียนที่ยังไม่กรอกเลขบัญชีจริงๆ)

👁️ **Pam ดู**: โชว์ตาราง preview ทุกแถว (ชื่อ + เลขบัญชี + จำนวน) ให้ Pam ไล่ตาดูว่าชื่อ-เลขบัญชีตรงกับที่รู้จักเด็กแต่ละคนจริงไหม โดยเฉพาะแถวไหนที่เลขบัญชีว่าง ให้ชี้ชัดว่าเป็นใคร

✅ **ผ่านเมื่อ**: Pam ยืนยันว่ารายชื่อ+เลขบัญชีถูกต้องครบ ไม่มีคนแปลกปลอม ไม่มีเลขบัญชีผิดคน

---

## Stage 4 — คำนวณยอดรวม + จำนวนผู้ฝาก

🔨 **สร้าง**: คำนวณ `totalAmount` (ผลรวม amount ทุกแถว) และ `depositorCount` (= จำนวนแถว)

🧪 **ทดสอบ**: รวมเลขด้วยมือจากตัวอย่าง preview ใน Stage 3 เทียบกับค่าที่ฟังก์ชันคำนวณได้ ต้องตรงเป๊ะ

👁️ **Pam ดู**: "ยอดรวมฝากวันนี้ = X บาท จาก Y คน" — ให้ Pam เทียบกับยอดที่รู้อยู่แล้วจากการฝากจริงวันนั้น (เช่น เทียบกับที่นับเงินสดได้จริง ถ้ามี)

✅ **ผ่านเมื่อ**: ยอดตรงกับที่ Pam คาด/นับไว้จริง

---

## Stage 5 — เรียงลำดับตามเวลาฝากจริง

🔨 **สร้าง**: เรียง `rows[]` ตาม `id` ธุรกรรม (epoch ms) จากน้อยไปมาก → ได้ลำดับการมาฝากจริงตามเวลา แล้วใส่เลข `ลำดับที่` 1,2,3... ต่อเนื่องไม่ซ้ำ

🧪 **ทดสอบ**: เช็คว่าเลขลำดับที่ 1 ถึง N ไม่ขาด ไม่ซ้ำ (ต่างจากไฟล์ตัวอย่างเดิมที่เจอเลข 23 ซ้ำ)

👁️ **Pam ดู**: โชว์ 5 แถวแรกกับ 5 แถวสุดท้ายพร้อมเลขลำดับที่ ให้ Pam ดูว่าลำดับเวลาสมเหตุสมผลไหม (เช่น คนแรกที่ฝากตอนเช้า ควรเป็นลำดับ 1)

✅ **ผ่านเมื่อ**: เลขลำดับต่อเนื่อง ไม่ซ้ำ ไม่ขาด และ Pam เห็นว่าลำดับเวลาสมเหตุสมผล

---

## Stage 6 — สร้าง Sheet1 จริง (ทะเบียนรับฝาก)

🔨 **สร้าง**: ใช้ผลจาก Stage 1-5 สร้าง Google Sheet ตามสเปคเต็มใน BLUEPRINT หัวข้อ 5 (หัวเรื่อง, header ตาราง, ข้อมูลทุกแถว, แถวรวม, formatting/border/สี ตาม `exportMonthly` pattern)

🧪 **ทดสอบ**: เปิด sheet ที่สร้างขึ้นเอง เช็คว่าจำนวนแถวตรง ผลรวมท้ายตารางตรงกับ Stage 4 ไม่มีเซลล์ว่างผิดที่ ไม่มี label หลุด/เยื้อง

👁️ **Pam ดู**: ผมจะส่งลิงก์ sheet ที่สร้างขึ้นจริงให้ Pam เปิดดูเอง เทียบหน้าตากับไฟล์ตัวอย่างเก่า (กรกฎาคม 2569) ว่าดูเป็นเอกสารแบบเดียวกันไหม ธนาคารน่าจะรับได้ไหม

✅ **ผ่านเมื่อ**: Pam เปิดดูแล้วบอกว่าหน้าตาโอเค พร้อมส่งธนาคารได้ (หรือชี้จุดที่ต้องแก้ให้เหมือนต้นฉบับมากขึ้น)

---

## Stage 7 — สร้าง Sheet2 จริง (ใบปะหน้านำฝาก)

🔨 **สร้าง**: สร้าง Sheet2 ตามสเปค BLUEPRINT หัวข้อ 6 — ตารางนับธนบัตร/เหรียญ (ช่องกรอกว่าง + สูตรคำนวณ), ยอด (1)/(2)/ผลต่าง เป็นสูตร, "จำนวนผู้ฝากทั้งหมด" ดึงจาก Stage 4, ลายเซ็นว่างไว้ให้เซ็นมือ

🧪 **ทดสอบ**: พิมพ์ตัวเลขทดสอบลงช่องจำนวนหน่วยเอง (เช่น ธนบัตร 1000 x 5 ใบ) เช็คว่าสูตร "จำนวนเงิน" และยอดรวม (1) คำนวณถูกจริงในสเปรดชีต แล้วลบตัวเลขทดสอบออกก่อนส่งให้ Pam ดู

👁️ **Pam ดู**: ส่งลิงก์ Sheet2 ให้ Pam ลองพิมพ์จำนวนธนบัตร/เหรียญจริงที่นับได้วันนั้น (ถ้ามีข้อมูลจริงอยู่แล้ว) แล้วดูว่ายอด (1) กับ (2) ตรงกัน ผลต่างออกมาเป็น 0 ไหม

✅ **ผ่านเมื่อ**: สูตรคำนวณถูกต้อง Pam ทดลองพิมพ์เองแล้วยอดตรงตามที่คาด

---

## Stage 8 — หน้า admin: ปุ่ม + ฟอร์ม

🔨 **สร้าง**: เพิ่ม tab ใหม่ `🏦 ทะเบียนฝากธนาคาร` ในหน้า admin (index.html) ตามสเปค BLUEPRINT หัวข้อ 7 — date picker, ช่องเลขที่เอกสาร, ช่องผู้รับเงิน, ปุ่มสร้าง, แสดงผลลัพธ์+ลิงก์หลังสร้างสำเร็จ, แสดง error ชัดเจนถ้าไม่พบธุรกรรม

🧪 **ทดสอบ**: ทดลองกดปุ่มจริงในเบราว์เซอร์ (เลือกวันที่มีข้อมูล, ไม่มีข้อมูล, ลืมกรอกช่อง) เช็คว่าแต่ละเคสแสดงผล/error ถูกต้อง

👁️ **Pam ดู**: ผมจะบอกขั้นตอนให้ Pam ลองกดเองในเว็บจริง (หลัง deploy แล้ว) เลือกวันที่ที่มีข้อมูลจริง แล้วดูว่าได้ลิงก์ sheet ที่ถูกต้อง

✅ **ผ่านเมื่อ**: Pam กดใช้งานจริงในเว็บได้ครบ flow ไม่มีจุดค้าง/error ที่ไม่คาดคิด

---

## Stage 9 — Edge cases sweep

🔨 **สร้าง**: ทดสอบครบทุกเคสใน BLUEPRINT หัวข้อ 8 (ไม่มีธุรกรรม, ไม่มีเลขบัญชี, นักเรียนถูกลบ, สร้างซ้ำวันเดียวกัน, มีทั้งฝากและถอนวันเดียวกัน)

🧪 **ทดสอบ**: รันแต่ละเคสจริงกับข้อมูลทดสอบ (สร้างธุรกรรมทดสอบชั่วคราวถ้าจำเป็น แล้วลบทิ้งหลังทดสอบ — **ห้ามทิ้งข้อมูลทดสอบปนกับข้อมูลจริง**)

👁️ **Pam ดู**: สรุปผลแต่ละเคสเป็นตารางสั้นๆ ("เคส X → ผลลัพธ์ Y ตรงตามที่ตกลงกันไว้ในข้อ Z ของ BLUEPRINT ไหม")

✅ **ผ่านเมื่อ**: ทุกเคสตรงตามที่ตกลงใน BLUEPRINT ไม่มีเคสไหน error แบบไม่คาดคิด

---

## Stage 10 — เทียบกับไฟล์จริงที่เคยส่งธนาคารแล้ว (hand-verify เต็มรูปแบบ)

🔨 **สร้าง**: ไม่มีโค้ดใหม่ — ทำ end-to-end validation เต็มรูปแบบ: ถ้ามีข้อมูลธุรกรรมของวันที่ตรงกับไฟล์ตัวอย่าง "กรกฎาคม 2569" ที่วิเคราะห์ไปแล้ว (หรือวันไหนก็ตามที่ Pam มีไฟล์ต้นฉบับที่ส่งธนาคารไปแล้วจริง) ให้สร้างทะเบียนจากระบบใหม่แล้วเทียบทีละแถวกับไฟล์ต้นฉบับ

🧪 **ทดสอบ**: เทียบยอดรวม, จำนวนคน, รายชื่อ ระหว่างไฟล์เก่ากับที่สร้างใหม่ — ถ้าไม่ตรง ต้องอธิบายได้ว่าต่างเพราะอะไร (เช่น ระบบใหม่ไม่รวมคนไม่ฝาก ตามที่ตกลงไว้ใน Decision Log #6 ไม่ใช่บั๊ก)

👁️ **Pam ดู**: ตารางเทียบ "เก่า vs ใหม่" พร้อมคำอธิบายทุกจุดที่ต่างกัน

✅ **ผ่านเมื่อ**: ทุกจุดต่างกันอธิบายได้ตาม Decision Log ไม่มีจุดต่างที่อธิบายไม่ได้ (= บั๊ก)

---

## Stage 11 — Deploy จริง

🔨 **สร้าง**: ไม่มีโค้ดใหม่ — เตรียม Code.gs ฉบับสมบูรณ์ให้ Pam paste เข้า GAS editor (`AKfycbzScb5FNx-...` deployment ที่ใช้งานจริงตอนนี้ — ยืนยัน URL ตรงกับ `var API` ใน index.html ก่อนเสมอ) แล้ว Deploy → Manage deployments → Edit → version ใหม่ → Update จากนั้น commit+push index.html (frontend)

🧪 **ทดสอบ**: หลัง Pam paste+deploy แล้ว ให้ทดลองกดใช้งานจริงในเว็บอีกครั้ง (ซ้ำ Stage 8 บนเว็บจริงหลัง deploy)

👁️ **Pam ดู**: ผลการใช้งานจริงครั้งแรกบนเว็บ production

✅ **ผ่านเมื่อ**: ใช้งานได้จริงบนเว็บ production ไม่ใช่แค่ในสภาพแวดล้อมทดสอบ

---

## PIVOT 2 — Stage 12-20 (แยกตามชั้น + กรองเฉพาะคนมีเลขบัญชี)

หลักการสร้าง: engine ก่อน interface เสมอ (Stage 12-14 = ตรรกะล้วน ไม่แตะการเขียนชีต, Stage 15-16 = เขียนชีตจริง) เพื่อให้รู้ทันทีว่าถ้าตัวเลขผิดคือ engine หรือ display

---

## Stage 12 — เพิ่มข้อมูลชั้น(snapshot)+เลขที่(join) เข้า group object

🔨 **สร้าง**: ในลูป join เดิม (ที่สร้าง `studMap`) เพิ่มอ่านคอลัมน์ "เลขที่" จากชีตนักเรียน (`findSH(['เลขที่'])`) เก็บเข้า `studMap[stuId].rollNo` ส่วน "ชั้น" **ไม่ใช้ studMap** แต่ดึงจาก snapshot ในชีตธุรกรรมเอง (`findTH(['ชั้น'])` มีคอลัมน์นี้อยู่แล้วจาก header `id, นักเรียน_id, ชื่อ, ชั้น, ประเภท, จำนวนเงิน, ปีการศึกษา, วันที่`) เก็บเข้า group ตอน group-by (ใช้ค่าจากธุรกรรมแรกสุดที่เจอของกลุ่มนั้น ตาม Decision Log #27)

🧪 **ทดสอบ**: จำลองข้อมูล (Node.js เหมือนที่ทำมาตลอด session นี้) — คนคนเดียวฝาก 2 ครั้งในเดือนเดียวกันแต่ transaction แรกกับหลังคนละชั้น (edge case คร่อมชั้น) ต้องได้ชั้นจากธุรกรรมแรก

👁️ **Pam ดู**: ไม่ต้องดูอะไร (engine stage ล้วน ไม่มีผลลัพธ์ที่เห็นได้จริง)

✅ **ผ่านเมื่อ**: assertion การจำลองผ่านหมด (ชั้น=จากธุรกรรมแรก, เลขที่=จาก join สด, นักเรียนถูกลบ→เลขที่ว่างไม่ error)

---

## Stage 13 — แยก accountRows/noAccountRows + totalAmount ใหม่ + edge case ทุกคนไม่มีบัญชี

🔨 **สร้าง**: หลัง group-by เสร็จ แยก rows เป็น 2 กลุ่มตาม `bankAccount` ว่าง/ไม่ว่าง คำนวณ `totalAmount`/`depositorCount` จาก **accountRows เท่านั้น** ถ้า `accountRows.length === 0` (แต่ `noAccountRows.length > 0`) → return error ทันทีตาม Decision Log #25 ไม่สร้างชีตไหนเลย

🧪 **ทดสอบ**: จำลอง 3 เคส — (ก) มีทั้งสองกลุ่มปนกัน (ข) ทุกคนมีบัญชี (noAccountRows ว่าง) (ค) ทุกคนไม่มีบัญชีเลย (ต้อง error ไม่สร้างชีต) — เช็ค totalAmount ของเคส (ก) ต้อง**ไม่รวม**ยอดของ noAccountRows

👁️ **Pam ดู**: ไม่ต้องดู (engine stage)

✅ **ผ่านเมื่อ**: ทั้ง 3 เคสได้ผลตรงตาม Decision Log #21, #25

---

## Stage 14 — เรียง accountRows ตามชั้น(`GRADES`)+เลขที่

🔨 **สร้าง**: sort ฟังก์ชันใหม่ — primary key: index ของชั้นใน `GRADES` array, secondary key: เลขที่ (ตัวเลข, คนไม่มีเลขที่ตกท้ายกลุ่มชั้นนั้น — pattern เดียวกับ `sortStudentsByRoll` ที่มีอยู่แล้วในระบบ) ใช้กับทั้ง accountRows (Stage 15) และ noAccountRows (Stage 16 แต่ไม่ต้องจัดกลุ่ม header)

🧪 **ทดสอบ**: จำลองข้อมูลผสมหลายชั้น + มีคนไม่มีเลขที่ปนอยู่ ตรวจว่าเรียงชั้นตามลำดับ `GRADES` เป๊ะ และในชั้นเดียวกันเลขที่น้อยอยู่ก่อน คนไม่มีเลขที่ตกท้ายชั้นนั้น (ไม่ใช่ท้ายสุดของทั้งตาราง)

👁️ **Pam ดู**: ไม่ต้องดู (engine stage)

✅ **ผ่านเมื่อ**: assertion ผ่านหมดรวมเคสคนไม่มีเลขที่

---

## Stage 15 — เขียน Sheet1 ใหม่แบบจัดกลุ่มต่อชั้น (จุดเสี่ยงสูงสุดของ pivot นี้)

🔨 **สร้าง**: เขียนใหม่ส่วนสร้าง Sheet1 — วนตาม `GRADES` ทีละชั้น ข้ามชั้นที่ไม่มีแถวใน accountRows เลย ถ้ามี: เขียนหัวข้อคั่น ("ชั้น ป.1" ตัวหนา พื้นสี) ตามด้วยแถวข้อมูล (ลำดับที่รีเซ็ตใหม่ทุกชั้นหรือต่อเนื่องทั้งไฟล์ — **ใช้ต่อเนื่องทั้งไฟล์** เพื่อให้นับจำนวนคนทั้งหมดง่าย) ตามด้วยแถวยอดรวมย่อย ("รวมชั้น ป.1 (n ราย): amount") จบด้วยแถวยอดรวมใหญ่เหมือนเดิม ระวัง: border/merge range ต้องคำนวณความสูงใหม่แบบ dynamic (ไม่ใช่ fixed แบบเดิม เพราะจำนวนชั้น+แถวต่อชั้นไม่คงที่)

🧪 **ทดสอบ**: hand-verify เต็มรูปแบบด้วยข้อมูลจริงของ Pam อย่างน้อย 1 เดือนที่มีคนฝากหลายชั้น — คำนวณยอดรวมย่อยแต่ละชั้นด้วยมือ (หรือ spreadsheet formula แยก) เทียบกับที่ระบบสร้าง ต้องตรงเป๊ะทุกชั้น และผลรวมย่อยทุกชั้นบวกกันต้องเท่ากับยอดรวมใหญ่

👁️ **Pam ดู**: เปิดชีตที่สร้างจริง เทียบยอดรวมย่อยแต่ละชั้นกับที่ Pam รู้เอง (เช่น "ชั้น ป.1 เดือนนี้ควรมีประมาณกี่คนกี่บาท")

✅ **ผ่านเมื่อ**: Pam ยืนยันว่ายอดย่อยแต่ละชั้น + ยอดรวมใหญ่ถูกต้องตรงกับที่รู้จริง — **นี่คือ stage ที่ต้องขอเวลา Pam ตรวจละเอียดที่สุดของ pivot นี้**

---

## Stage 16 — สร้าง Sheet3 "ไม่มีบัญชี_M-Y" (ถ้ามี noAccountRows)

🔨 **สร้าง**: ถ้า `noAccountRows.length > 0` สร้างชีตใหม่ชื่อ `ไม่มีบัญชี_M-Y` ตาราง flat (ไม่มีหัวข้อคั่นชั้น ไม่มี subtotal) คอลัมน์: ลำดับที่, ชื่อ, ชั้น, เลขที่, ยอดสะสมเดือนนี้, หมายเหตุ (รวม N ครั้ง) เรียงตามชั้น+เลขที่เหมือน Stage 14 ถ้า `noAccountRows.length === 0` **ไม่สร้างชีตนี้เลย** (ไม่ใช่สร้างชีตเปล่า)

🧪 **ทดสอบ**: จำลอง 2 เคส (มี/ไม่มี noAccountRows) เช็คว่าชีตถูกสร้าง/ไม่ถูกสร้างตรงเงื่อนไข และคอลัมน์/การเรียงถูกต้อง

👁️ **Pam ดู**: เปิดชีตนี้ (ถ้าถูกสร้าง) เช็คว่ารายชื่อที่โผล่มาคือคนที่ Pam รู้อยู่แล้วว่ายังไม่มีบัญชีจริงๆ

✅ **ผ่านเมื่อ**: Pam ยืนยันรายชื่อในชีตนี้ตรงกับที่รู้จริง

---

## Stage 17 — อัปเดต response + ข้อความฝั่ง frontend

🔨 **สร้าง**: เพิ่ม `sheet3Name` (string หรือ `null`) ใน return ของ `generateDepositRegistry()` ฝั่ง `submitBk()` ใน index.html เพิ่มบรรทัดแสดงถ้า `r.sheet3Name` ไม่ null บอกจำนวนคนไม่มีบัญชีด้วย (ดึงจาก response เพิ่ม `noAccountCount`)

🧪 **ทดสอบ**: syntax check (`node --check`) ทั้ง Code.gs และ inline script ของ index.html เหมือนทุกครั้งที่ผ่านมาใน session นี้

👁️ **Pam ดู**: ข้อความสำเร็จหลังกดสร้างจริง ต้องเห็นชัดว่ามีกี่คนไม่มีบัญชี

✅ **ผ่านเมื่อ**: ข้อความแสดงครบทั้ง 3 ชีต (หรือ 2 ถ้าไม่มีคนไม่มีบัญชี) ไม่มี syntax error

---

## Stage 18 — Edge case sweep รอบใหม่

🔨 **สร้าง**: ทดสอบเคสเฉพาะของ pivot นี้: ชั้นที่ไม่มีใครฝากเลย (ต้องไม่โผล่หัวข้อเปล่า), ทุกคนไม่มีบัญชี (ต้อง error ไม่สร้างชีตไหนเลย), ทุกคนมีบัญชีหมด (ไม่สร้าง Sheet3), คนเดียวคร่อม 2 ชั้น

🧪 **ทดสอบ**: รันจำลองครบทุกเคสอีกรอบรวมกับของเดิม (Stage 9 เดิม) ให้แน่ใจไม่มี regression

👁️ **Pam ดู**: สรุปผลแต่ละเคสเป็นตารางสั้นๆ

✅ **ผ่านเมื่อ**: ทุกเคสตรงตาม Decision Log ไม่มี error ที่ไม่คาดคิด

---

## Stage 19 — เทียบกับข้อมูลจริงเต็มรูปแบบ (hand-verify)

🔨 **สร้าง**: ไม่มีโค้ดใหม่ — สร้างทะเบียนจริงจากเดือนที่ Pam มีข้อมูลครบ (มีทั้งคนมี/ไม่มีบัญชี หลายชั้น) เทียบกับสิ่งที่ Pam คาดหวังจากความรู้จริง

🧪 **ทดสอบ**: เทียบยอดรวมย่อยต่อชั้น, ยอดรวมใหญ่, รายชื่อใน Sheet3 — ทุกจุดต่างต้องอธิบายได้

👁️ **Pam ดู**: ตารางเทียบ "คาดหวัง vs ผลจริง"

✅ **ผ่านเมื่อ**: ไม่มีจุดต่างที่อธิบายไม่ได้

---

## Stage 20 — Deploy จริงอีกรอบ

🔨 **สร้าง**: paste Code.gs ฉบับสมบูรณ์เข้า GAS editor → Deploy → Manage deployments → Edit deployment เดิม (`AKfycbzScb5FNx-...`) → version "New" → Update → commit+push index.html

🧪 **ทดสอบ**: ทดลองกดใช้งานจริงบนเว็บ production อีกครั้ง (ซ้ำ Stage 15-16 บนเว็บจริง)

👁️ **Pam ดู**: ผลการใช้งานจริงครั้งแรกบนเว็บ production ของ pivot นี้

✅ **ผ่านเมื่อ**: ใช้งานได้จริงบนเว็บ production

---

## สิ่งที่ต้องการจาก Pam ระหว่างสร้าง

- Stage 2, 3, 6, 7, 10 (v1.0/v1.1) เป็นจุดที่ต้องให้ Pam ช่วยยืนยันกับความจริงที่ Pam รู้เอง (ชื่อเด็ก, เลขบัญชี, ยอดเงินสดจริง) — ผมทำเองไม่ได้
- Stage 2 (การ match วันที่) และ Stage 10 คือจุดเสี่ยงสูงสุดของ v1.0/v1.1
- **Stage 15 (จัดกลุ่ม Sheet1 ตามชั้น) คือจุดเสี่ยงสูงสุดของ PIVOT 2** — ขอเวลา Pam ตรวจยอดรวมย่อยแต่ละชั้นละเอียดเป็นพิเศษ
- Stage 16 ต้องให้ Pam ยืนยันว่ารายชื่อ "ไม่มีบัญชี" ตรงกับที่รู้จริง (ผมเช็คจากโค้ดอย่างเดียวไม่ได้ว่าใครควรมีบัญชีจริงๆ)

## จุดเสี่ยงที่ต้องระวังเป็นพิเศษ

1. **รูปแบบวันที่ไทย** ("D MMM YYYY" ไม่มี leading zero, เดือนต้องตรง `THAI_MONTHS` เป๊ะ) — ถ้า format ผิดแม้แต่นิดเดียว การกรองธุรกรรมจะได้ 0 แถวเงียบๆ โดยไม่มี error (เจอจริงมาแล้วรอบ Date object bug — ดู BLUEPRINT PIVOT #17)
2. **เลขบัญชีว่าง** — ต้องเว้นว่างได้โดยไม่ทำให้ทั้งแถวหาย (v1.0/v1.1) — ตอนนี้ (PIVOT 2) เปลี่ยนเป็นตัดออกจาก Sheet1 แทน ต้องระวังไม่ให้ "ตัดออก" กลายเป็น "หายไปเงียบๆ ไม่มีที่ไปเลย" (ต้องไปโผล่ Sheet3 เสมอ)
3. **สูตรใน Sheet2** ต้องอ้างอิงเซลล์ถูกตำแหน่งเป๊ะ (ถ้า Pam แทรก/ลบแถวทีหลัง สูตรอาจเพี้ยน — ควรใส่หมายเหตุเตือนไว้ในชีตว่า "ห้ามแทรกแถวในตารางนี้")
4. **ชื่อ sheet ซ้ำ** เมื่อสร้างทะเบียนเดือนเดียวกันซ้ำ — Sheet1/Sheet3 ถูกลบทิ้งแบบไม่เตือน (ไม่มีข้อมูลมือกรอกจึงปลอดภัย) แต่ Sheet2 มี guard กันลบทับอยู่แล้ว (v1.1)
5. **[ใหม่] border/merge range แบบ dynamic ใน Stage 15** — เสี่ยง off-by-one สูงสุดของ pivot นี้ เพราะความสูงของแต่ละส่วนไม่คงที่อีกต่อไป (แตกต่างจากเดิมที่เป็น flat list คำนวณ range ง่าย) ต้องทดสอบกับข้อมูลจริงหลายชั้นจริงๆ ไม่ใช่แค่ mock 1-2 แถว
6. **[ใหม่] ชั้น snapshot vs ชั้นปัจจุบัน** — ถ้า Pam สงสัยว่าทำไมนักเรียนไปโผล่ชั้นที่ "ผิด" (เช่น เพิ่งเลื่อนชั้นไปแล้ว) ให้อธิบายว่านี่คือชั้น ณ ตอนฝากจริง ไม่ใช่ชั้นปัจจุบัน (Decision Log #20) ไม่ใช่บั๊ก

## Deliverables

- `Code.gs` เพิ่มฟังก์ชัน `generateDepositRegistry()` + action routing (ระบบออมทรัพย์) — v1.0/v1.1 ship แล้ว, PIVOT 2 รอสร้าง
- `index.html` เพิ่ม tab admin ใหม่ (ระบบออมทรัพย์) — v1.0/v1.1 ship แล้ว, PIVOT 2 รอสร้าง
- `BLUEPRINT-bank-deposit-registry.md` + `CONSTRUCTION_PLAN-bank-deposit-registry.md` (เอกสารนี้) เป็นบันทึกถาวรของการออกแบบทั้งหมด (v1.0 → v1.1 → PIVOT 2)
